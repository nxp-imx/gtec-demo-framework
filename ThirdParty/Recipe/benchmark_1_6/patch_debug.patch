diff --git a/src/CMakeLists.txt b//srcCMakeLists.txt
index e814a4e..58ea2d6 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -24,6 +24,10 @@ set_target_properties(benchmark PROPERTIES
   VERSION ${GENERIC_LIB_VERSION}
   SOVERSION ${GENERIC_LIB_SOVERSION}
 )
+if(MSVC)
+    set_property(TARGET benchmark PROPERTY DEBUG_POSTFIX d)
+endif()
+
 target_include_directories(benchmark PUBLIC
   $<BUILD_INTERFACE:${PROJECT_SOURCE_DIR}/include>)
 
@@ -61,6 +65,9 @@ set_target_properties(benchmark_main PROPERTIES
   VERSION ${GENERIC_LIB_VERSION}
   SOVERSION ${GENERIC_LIB_SOVERSION}
 )
+if(MSVC)
+    set_property(TARGET benchmark_main PROPERTY DEBUG_POSTFIX d)
+endif()
 target_link_libraries(benchmark_main PUBLIC benchmark::benchmark)
 
 
